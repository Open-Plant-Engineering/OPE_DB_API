name: CI

on:
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run domain self-tests
        run: |
          python scripts/run_tests.py

      - name: Install tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff black

      - name: Lint
        run: ruff src tests

      - name: Format check
        run: black --check src tests
